# API key for authenticating requests to TalkingDB endpoints.
# If not set, authentication is bypassed (dev mode).
# Generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
TDB_API_KEY=

# Maximum upload file size in MB (default: 50)
TDB_MAX_FILE_SIZE_MB=50
